# Local development fallback values
# IMPORTANT: In Railway, configure MONGO_URL as an environment variable
# pointing to a real MongoDB instance (MongoDB Atlas or Railway MongoDB plugin)
MONGO_URL="mongodb://localhost:27017"
DB_NAME="educando_db"
CORS_ORIGINS="*"