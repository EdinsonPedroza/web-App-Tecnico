# MongoDB Configuration
# IMPORTANTE: Para seguridad, configura estas variables usando variables de entorno del sistema
# en lugar de incluir credenciales reales en este archivo si el repositorio es público

# Opción 1: Variables de entorno del sistema (RECOMENDADO)
# Linux/Mac: export MONGO_URL="mongodb+srv://usuario:password@cluster.mongodb.net/webApp"
# Windows: set MONGO_URL=mongodb+srv://usuario:password@cluster.mongodb.net/webApp

# Opción 2: Descomenta y configura aquí solo para desarrollo local privado
# MONGO_URL="mongodb+srv://usuario:password@cluster.mongodb.net/webApp?appName=Cluster0"

# Para desarrollo local con Docker:
# MONGO_URL="mongodb://mongo:27017"

DB_NAME="WebApp"
CORS_ORIGINS="*"

# Password Storage Mode: 'bcrypt' or 'plain'
# 'bcrypt' = Hash passwords with bcrypt (SECURE, recommended for production)
# 'plain' = Store passwords as plain text (INSECURE, only for backwards compatibility)
# Note: The system can verify both formats, so changing this only affects NEW passwords
PASSWORD_STORAGE_MODE="bcrypt"

# INSTRUCCIONES PARA USAR TU BASE DE DATOS:
# 1. NO incluyas credenciales reales en este archivo si el repo es público
# 2. Usa el script de configuración: ./configurar_mongodb.sh
# 3. O usa variables de entorno del sistema (ver arriba)
# 4. O crea un archivo .env.local (no rastreado por git) con tu URL real
# 5. Ver CONFIGURACION_MONGODB.md para más detalles
