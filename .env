# IMPORTANT: Configure these environment variables in Railway's dashboard:
# - MONGO_URL: MongoDB connection string (NOT localhost - use MongoDB Atlas or Railway MongoDB plugin)
# - DB_NAME: educando_db (or your preferred database name)
# - JWT_SECRET: A long, random, secure secret key for JWT tokens
# - CORS_ORIGINS: * (or your specific domain for production)
# - PORT: Automatically set by Railway (do not configure manually)

# Local development values (Railway will override these)
DOMAIN_URL=https://tudominio.com
JWT_SECRET=cambia_esta_clave_por_una_muy_segura_y_larga
